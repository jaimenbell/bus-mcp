"""bus-mcp -- an MCP server fronting the AlphaHive coordination-bus HTTP API.

`__version__` is one of three copies of this number (pyproject.toml and
server.json hold the others); tests/test_version.py pins them together so
they cannot drift.
"""

__version__ = "0.2.1"
